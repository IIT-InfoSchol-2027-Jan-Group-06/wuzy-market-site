---
const { n, t, wide = false } = Astro.props;
---
<div class:list={['reveal', wide ? 'md:pl-24' : '']}>
  <p class="font-display text-[clamp(3.5rem,12vw,9rem)] leading-none text-butter">{n}</p>
  <p class="mt-4 max-w-lg text-lg leading-relaxed text-mute">{t}</p>
</div>
